- Discover runtime tool catalog and MCP `tools/list`; choose exact callable name and schema before action; never infer missing tool.
- Next.js: run `next dev`; invoke configured `next-devtools` MCP (`npx -y next-devtools-mcp@latest`); call `tools/list`; require `get_compilation_issues`; then call `get_project_metadata`, `get_routes`, `get_page_metadata`, `get_errors`; use `agent-browser skills get core` before browser checks.
- Context7 MCP: call `mcp__context7__resolve_library_id`; select official/high-quality library ID; call `mcp__context7__query_docs` one topic per query; use returned sources.
- shadcn: run `npx shadcn@latest info --json`; then `npx shadcn@latest search <query>`; then `npx shadcn@latest docs <component>` and fetch returned URLs; then `npx shadcn@latest view <component>` or `npx shadcn@latest add <component> --dry-run` plus `--diff <file>`; only then `npx shadcn@latest add <component>`; inspect changed files.
- Vercel docs: call `mcp__codex_apps__vercel_search_vercel_documentation({topic,tokens})`; follow returned documentation links; never replace tool output with remembered or local Markdown.
- Vercel diagnostics: call `mcp__codex_apps__vercel_list_teams({})`; then `mcp__codex_apps__vercel_list_projects({teamId})`; then `mcp__codex_apps__vercel_list_deployments({teamId,projectId})`; then `mcp__codex_apps__vercel_get_deployment({teamId,idOrUrl})`; then `mcp__codex_apps__vercel_get_deployment_build_logs({teamId,idOrUrl})` or `mcp__codex_apps__vercel_get_runtime_errors({teamId,projectId})`; deploy only after explicit user approval.
- Any tool missing, failing, empty, or returning unexpected output: STOP; report exact result; state unknowns; ask approval; no silent fallback, grep, Markdown substitution, edits, or claims of completion.
- Evidence: claim only invoked-tool output; label `Verified`, `Inferred`, or `Unknown`; never claim skill, MCP, audit, or visual review without invocation evidence.
