import { ResponsiveBrandEmblem } from "@/components/brand/ResponsiveBrandEmblem"
import Link from "next/link"
import { Button } from "@/components/ui/button"
import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@/components/ui/collapsible"
import { NavigationMenu, NavigationMenuItem, NavigationMenuLink, NavigationMenuList } from "@/components/ui/navigation-menu"

<section id="logo-family" className="material-surface material-cork mt-5 rounded-2xl border border-border p-5 sm:p-8 lg:p-12">
  <p className="mb-2 text-xs font-black uppercase tracking-[.13em] text-primary">Logo production family</p>
  <h2 className="m-0 font-heading text-3xl sm:text-5xl">One identity, sized for its job</h2>
  <p className="max-w-3xl leading-7">Use <code>ResponsiveBrandEmblem</code> as the default identity control. The same component selects the compact 32 px mark in the mobile header and the 44 px navigation mark when space permits; explicit card and detail roles keep their approved exports inside the component.</p>

  <div className="material-surface material-leather-indigo mt-7 flex min-w-0 flex-wrap items-center gap-3 rounded-xl border border-border p-3 sm:px-4">
    <Link className="inline-flex min-h-11 min-w-0 flex-1 items-center gap-2" href="/" aria-label="Old MacDonald Had a School home">
      <ResponsiveBrandEmblem className="shrink-0" />
      <span className="min-w-0 font-heading text-sm sm:text-base">Old MacDonald Had a School</span>
    </Link>
    <NavigationMenu className="hidden sm:flex" viewport={false} aria-label="Header navigation example">
      <NavigationMenuList>
        <NavigationMenuItem><NavigationMenuLink asChild><Link href="/lessons">Lessons</Link></NavigationMenuLink></NavigationMenuItem>
        <NavigationMenuItem><NavigationMenuLink asChild><Link href="/grade/grade-two">Grades</Link></NavigationMenuLink></NavigationMenuItem>
      </NavigationMenuList>
    </NavigationMenu>
  </div>

  <div className="mt-5 grid min-w-0 gap-4 lg:grid-cols-2">
    <article className="material-surface material-cardboard-paper min-w-0 rounded-xl border border-border p-5">
      <div className="flex min-w-0 flex-wrap items-center gap-4">
        <ResponsiveBrandEmblem variant="card" className="size-16 shrink-0 object-contain sm:size-24" />
        <div className="min-w-0 flex-1"><h3 className="m-0 font-heading text-2xl">Card identity</h3><p className="mb-0">Use the card role beside a grouped teacher action, not as a detached logo specimen.</p></div>
        <Button asChild><Link href="/grade/grade-two">Open Grade 2</Link></Button>
      </div>
    </article>
    <article className="material-surface material-cardboard-paper min-w-0 rounded-xl border border-border p-5">
      <div className="flex min-w-0 flex-wrap items-center gap-4">
        <ResponsiveBrandEmblem variant="detail" className="size-16 shrink-0 object-contain sm:size-24" />
        <div className="min-w-0 flex-1"><h3 className="m-0 font-heading text-2xl">Detail media</h3><p className="mb-0">Use the detail role in an editorial or print-scale destination; keep the action a shared button.</p></div>
        <Button asChild variant="outline"><Link href="/about">About the school</Link></Button>
      </div>
    </article>
  </div>

  <Collapsible className="mt-5">
    <CollapsibleTrigger asChild><Button variant="outline" type="button">Show logo usage</Button></CollapsibleTrigger>
    <CollapsibleContent className="mt-3 min-w-0 overflow-x-auto rounded-xl border border-border bg-card p-4 text-card-foreground">
      <pre className="m-0 min-w-max text-sm"><code>{`<ResponsiveBrandEmblem />
<ResponsiveBrandEmblem variant="card" />
<ResponsiveBrandEmblem variant="detail" />`}</code></pre>
    </CollapsibleContent>
  </Collapsible>
</section>
