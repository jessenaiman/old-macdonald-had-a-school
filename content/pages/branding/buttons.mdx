import { CharacterActionExample } from "../../../components/brand/BrandSystemExamples"

<section id="buttons" className="mt-5 rounded-2xl border border-border bg-background p-5 sm:p-8 lg:p-12">

<p className="mb-2 text-xs font-black uppercase tracking-[.13em] text-primary">Character-local action pattern</p>
<h2 className="m-0 font-heading text-[clamp(2rem,4vw,3.5rem)]">Actions belong to a teaching context</h2>

Buttons are not a detached variant wall. Primary, secondary, and quiet actions should express the next useful teacher decision while character colour remains local to the character's own surface.

<div className="mt-7"><CharacterActionExample /></div>

</section>
