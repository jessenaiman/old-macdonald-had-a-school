import { PaletteReference } from "@/components/brand/PaletteReference"

<section id="palette" className="scroll-mt-24">

## Palette in use

The swatches below are live values from `theme.css`, not a copied palette. Site surfaces respond to the selected theme. Curriculum, grade, and character identities remain explicit semantic roles. Follow the links to see each role used in context.

<PaletteReference />

</section>
