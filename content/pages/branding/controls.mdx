import Link from "next/link"
import { GradeControlsReference } from "../../../components/grades/GradeControlsReference"

<section id="controls" className="material-surface material-cork mt-5 rounded-2xl border border-border p-5 sm:p-8 lg:p-12">

<p className="mb-2 text-xs font-black uppercase tracking-[.13em] text-primary">Production control pattern</p>
<h2 className="m-0 font-heading text-3xl sm:text-5xl">Shared controls, composed as a teacher task</h2>

This is the actual shared grade workspace used by the grade routes. Its navigation, buttons, cards, search input, results, and preview dialog are production controls rather than branding-only specimens. <Link className="font-bold underline underline-offset-4" href="/branding#controls">Open the focused component rules</Link>.

<div className="mt-7"><GradeControlsReference /></div>

Use the shared primitives inside production components. Do not create a page-local button, input, tab bar, disclosure, or visual control wall.

</section>
