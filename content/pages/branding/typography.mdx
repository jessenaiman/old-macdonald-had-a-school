import Link from "next/link"
import { LessonTypographyExample } from "../../../components/brand/BrandSystemExamples"

<section id="typography" className="mt-5 rounded-2xl border border-border bg-[#dce7ea] p-[clamp(1.25rem,4vw,3rem)]">

<p className="mb-2 text-xs font-black uppercase tracking-[.13em] text-primary">Production type pattern</p>
<h2 className="m-0 font-heading text-[clamp(2rem,4vw,3.5rem)]">Typography inside a real lesson</h2>

The three type roles work together inside a production composition. Copy the role and hierarchy—not an isolated font specimen. <Link className="font-bold underline underline-offset-4" href="/branding/PALETTE_AND_TYPOGRAPHY.md">Open the focused rules</Link>.

<div className="mt-7"><LessonTypographyExample /></div>

</section>
