import { Button } from "@/components/ui/button"
import { HomeSubjectNote } from "@/components/home/HomeSubjectNote"
import Link from "next/link"

<section id="homepage-surface" className="material-surface material-leather-blue mt-5 rounded-2xl border border-border p-5 sm:p-8 lg:p-12">

<h2 className="m-0 font-heading text-[clamp(1.875rem,4vw,3rem)] font-normal leading-none text-balance text-primary-foreground">Homepage structural surface</h2>

The menu, hero, and homepage base use the registered `material-surface material-leather-blue` recipe backed by the repeating `--asset-leather-blue` tile. Paper lesson lists and headings are clipped or taped to leather. The subject area is a cork bulletin board holding the six authored paper-note assets. Grade and folk links are locally identified repeating cloth badges.

<div className="mt-6">
  <Button asChild><Link href="/">Open the production homepage</Link></Button>
</div>

<p className="mt-4 text-primary-foreground"><strong>One production route, one source of homepage content.</strong> <code>app/page.tsx</code> owns the homepage headings, labels, links, and content-left/media-right composition. The small client carousel owns interaction only; theme recipes own colour, border, foreground, paper, and textile; <code>brand-assets.css</code> owns the four scene files.</p>

<div className="mt-6 grid items-start gap-6 lg:grid-cols-[minmax(0,.72fr)_minmax(18rem,1fr)]">
  <article className="material-surface material-cardboard-paper relative rounded-xl border border-border p-6 pt-10 text-card-foreground shadow-lg">
    <span className="brand-asset fastener-paperclip icon-small absolute -top-4 left-6" aria-hidden="true" />
    <h3 className="m-0 font-heading text-xl font-normal leading-none text-balance">Controls stay semantic</h3>
    <p className="mt-3 leading-7">Shared buttons use semantic control tokens. Character textures belong only to character-owned compositions.</p>
    <div className="mt-5 flex flex-wrap gap-3">
      <Button>Find lessons</Button>
      <Button variant="secondary">Open planner</Button>
      <Button variant="outline">Clear choices</Button>
    </div>
  </article>

  <div className="material-surface material-cork rounded-xl border border-border p-5 pt-8">
    <HomeSubjectNote
      subject="language"
      title="Language & literacy"
      href="/search?q=language%20literacy"
      iconClass="drama-storytelling-icon"
      teacherReason="Build confident talk, reading, and storytelling."
      highlights={["Build vocabulary", "Explore phonics", "Tell and retell stories"]}
      fastenerClass="fastener-paperclip"
      guideCharacter="whiskers"
    />
  </div>
</div>

<h3 className="mt-8 font-heading text-xl font-normal leading-none text-balance text-primary-foreground">Extension rule</h3>

Palette values, semantic theme tokens, and reusable global utilities belong in `globals.css`. Approved asset paths belong in `brand-assets.css`. `app/page.tsx` owns the production homepage composition and content; `HomeCarousel` owns only the required client-side carousel behavior. This page documents the registered classes; it must not introduce an alternate homepage control.

</section>
