import { BrandingIconCatalogue } from "@/components/brand/BrandingIconCatalogue"

<section id="icons" className="material-surface material-cork mt-5 rounded-2xl border border-border p-5 sm:p-8 lg:p-12">
  <p className="mb-2 text-xs font-black uppercase tracking-[.13em] text-primary">Semantic icon catalogue</p><h2 className="m-0 font-heading text-[clamp(2rem,4vw,3.5rem)]">Curriculum signals already available</h2><p className="max-w-3xl leading-7">This is the canonical inclusion guide. Other branding examples link to the exact icon here instead of repeating instructions. Grade colour and grade artwork remain separate.</p>
  <div className="mt-5 rounded-xl border border-border bg-muted p-4 text-muted-foreground"><strong className="text-foreground">Use one semantic class:</strong><code className="mt-2 block overflow-x-auto rounded-md border border-border bg-card p-3 text-foreground [background-image:var(--brand-paper-texture)] [background-size:260px]">{`<span className="brand-asset music-icon icon-medium" aria-hidden="true" />`}</code><p className="mb-0 mt-2 text-sm">Hover or focus an icon to see its ready-to-use semantic HTML. For grade artwork, combine <code>brand-asset grade-icon</code> with the documented <code>data-grade-icon</code>. Use <code>icon-small</code>, <code>icon-medium</code>, or <code>icon-large</code>; never paste an asset URL into a page component.</p></div>
  <BrandingIconCatalogue />
</section>
