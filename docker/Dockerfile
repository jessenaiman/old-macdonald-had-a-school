FROM postgres:16-alpine

# For now, just use standard PostgreSQL without pgvector
# We can add pgvector later if needed for vector search
# The local development will work fine without vector embeddings
