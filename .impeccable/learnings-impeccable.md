# learnings.md — impeccable

Read at the start of any use of this skill; append one dated bullet at the end. Merge, don't duplicate. Prune past ~15 bullets.

**This file is canonical at `.impeccable/learnings-impeccable.md`.** `.agents/skills/impeccable/` is an install: `npx impeccable update` deletes everything in it, this file included. A copy sits there for the read-at-start convention only; edit this one.

- 2026-09-19: **Check in often, and always before a destructive task.** Overwriting DESIGN.md, deleting files, updating the skill, replacing a design: name exactly what is destroyed and get a yes first — and raise the check-in in the same message as the intent, never deferred. This session overwrote DESIGN.md and ran a mid-session skill update without either.
- 2026-09-19: The roll's assignment is not a suggestion. `concept-seed` said so: "The assignment is the roll, not a suggestion." Present it committed and offer a re-roll; if the user does not re-roll, the world is committed and the next phase starts without asking again.
- 2026-09-19: Colour strategy governs how much of their **component** space colour owns, not how much of the page. It is design's creative call — decide it and show the result rather than putting it to the user as a question.
- 2026-09-19: **Delegation rule (owner-locked, ai-dev-kit README).** Learn and implement from the cheapest models; never delegate to Claude, Astra, Sol, or any non-flash/expensive model. Allowed: `deepseek/deepseek-v4.1-flash`, `xiaomi/mimo-v2.5`, `deepseek/deepseek-v4-flash`, `inclusionai/ling-3.0-flash-vl:free`, `nvidia/nemotron-3-ultra-550b-a55b:free`, `typesafe/jev-1.13`. Expensive tasks are human-delegated, never agent-delegated. Breached once: the clean-room build went to `openai-codex/gpt-5.6-terra`.
- 2026-09-19: **Never run `npx impeccable update` mid-session.** The boot text says so outright ("it rewrites the skill files this session is reading… run it in a later turn"), and the cost is concrete: it deleted `learnings.md`, replaced every `scripts/*.mjs` with the native launcher so all cited command lines died, and shifted the Setup lines under citations just verified.
- 2026-09-19: **Correct setup is exactly one `context` run per session, with `--target`.** Line 17 forbids rerunning. A rerun only ever exists to repair a missing target, so read the request for a named route or file *before* the first call.
- 2026-09-19: Updated to 4.3.1. Commands are now `<skill-base-dir>/scripts/impeccable <verb>`, or `impeccable.cmd` on a Windows shell without `sh`. Any `node .../scripts/*.mjs` citation — context, doctor, hook-admin, concept-seed, pin, detect — is obsolete.
- 2026-09-19: In 4.3.1 the Setup items are lines 17, 18, 19 (`## Setup` at 15), and the version lives at `metadata.version`, not a top-level `version:` key.
- 2026-09-19: Setup step 1 (`SKILL.md:17`) must be declared in chat with a clearly visible emoji. Without the marker the user cannot tell a skipped setup from a silent one.
- 2026-09-19: Pass `--target <path>` whenever the request names a route or file. Without it the brief is chosen by fallback (`surfaceBriefReason: "only-brief"`); with it, by `"mapping"` against the brief's `primary_target`. A one-brief project hides the omission until a second surface exists.
- 2026-09-19: The boot output is ~28 KB and re-prints DESIGN.md, the sidecar and the surface brief in full. Redirect it to a file and read only the directive lines (`CONTEXT_STALE`, `UPDATE_AVAILABLE`, `IMAGE_TOOLS`, `RESOLVED_CONTEXT`).
- 2026-09-19: If the launcher refuses or fails, `SKILL.md:21` requires a separate message **before the next tool call** — "Context loading did not run; I'll read the existing project context directly" — then read PRODUCT.md and DESIGN.md rather than inventing missing context.
- 2026-09-19: Setup step 2 is not satisfied by reading a reference once. The playbook that owns the request must be loaded for that request — `shape.md` for planning, `document.md` for DESIGN.md, `new-work.md` for a new or replacement world — with the target and one source of incumbent visual truth inspected before any edit.
